Validar se o terminal possui acesso a AWS

==> Forma número 1

Instalar o awscli:

apt install awscli

Roda o:

aws configure

Valida se deu tudo certo com o comando abaixo:

aws sts get-caller-identity


==> Forma número 2 (mais segura)

utilizando role que é uma identidade atribuída a um serviço. No caso aqui atribuiremos uma role ao terraform para que ele possa criar recursos na AWS.